
<scroll-view scroll-x="true" wx:for="{{goods_list}}" class="van-panel van-hairline--top-bottom custom-class" style='margin-bottom:35rpx;'>
<view style='display:flex;white-space: nowrap;width:130%'>
  <view style="width:100%;display:inline-block;">
    <van-cell title="{{ '[出售]'+item.title }}" label="" value="" custom-class="header-class" value-class="van-panel__header-value" bindtap="goto" />
    <view class="van-panel__content" bindtap="goto">
      <slot />
      <view style='display:flex;flex-wrap:wrap;'>
        <view class='inline' style='display:flex;justify-content:space-around;padding:25rpx 0rpx;width:100%'>
          <image src='{{item.pic}}' style='width:200rpx;height:200rpx;'></image>
          <image src='{{item.pic}}' style='width:200rpx;height:200rpx;'></image>
          <image src='{{item.pic}}' style='width:200rpx;height:200rpx;'></image>
        </view>
      </view>
    </view>

    <view class="van-panel__footer2 van-hairline--top footer-class" style="display:flex;justify-content:space-between;align-items:center;font-size:26rpx;color:#20262e" bindtap="goto">
      <view style='color:#1173bc;font-size:36rpx'>¥{{item.price}}</view>


      <view style='display:flex;align-items:center;color:#5b5a5a'>
        <i-icon type="coordinates" size="22" />
        <view>{{item.place}}</view>
      </view>
      <view style='color:#ccc'>1分钟前</view>
    </view>
  </view>
  <view style="align-self:stretch;width:30%;display:inline-flex;align-items:center;justify-content:center"><van-button type="danger" bindtap="askdel" >删除发布</van-button></view>
  </view>
</scroll-view>


<!--
<view class="weui-panel weui-panel_access">
  <view class="weui-panel__hd">我的发布</view>
  <view class="weui-panel__bd">
    <navigator wx:for="{{goods_list}}" url="/pages/detail/detail?id={{item.id}}" class="weui-media-box weui-media-box_appmsg" hover-class="weui-cell_active">
      <view class="weui-media-box__hd weui-media-box__hd_in-appmsg">
        <image class="weui-media-box__thumb" src="{{item.pic}}" />
      </view>
      <view class="weui-media-box__bd weui-media-box__bd_in-appmsg">
        <view class="weui-media-box__title">{{item.status?"[已通过]":"[未过审]"}}{{item.title}}</view>
        <view class="weui-media-box__desc">{{item.description}}</view>
      </view>
    </navigator>
  </view>

</view>

<!--
  <i-collapse name="{{name}}">
    <i-collapse-item wx:for="{{goods_list}}" title="{{item.status?'[已通过]':'[未过审]'}}{{item.title}}" category="电子设备" name="name1" i-class="issue_title">
      <view slot="content" >
      <image class="issue_img" src="{{item.pic}}" />
      <view style='display:block'>  {{item.description}}</view>
      
      </view>
    </i-collapse-item>
    </i-collapse>


  <view class="weui-cells__title" style='margin-top:-15rpx;font-size:28rpx'>近期发布</view>


    <i-collapse name="{{name}}">
      <i-collapse-item wx:for="{{goods_list}}" title="{{item.status?'':'[未过审]'}}{{item.title}}" category="电子设备" name="name1" i-class="issue_title" icontype="{{item.status?'':'prompt'}}">
        <view slot="content">
          <image class="issue_img" src="{{item.pic}}" />
          <view style='display:block'> {{item.description}}</view>

        </view>
      </i-collapse-item>
    </i-collapse>

-->